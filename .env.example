# Copy to .env (gitignored) and fill in. Used by scripts/ai_writer.py.
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional — defaults to gpt-5.
OPENAI_MODEL=gpt-5
# Optional — output token cap. Defaults to 16384.
OPENAI_MAX_TOKENS=16384
