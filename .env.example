# Copy to .env (gitignored) and fill in. Used by scripts/ai_writer.py.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional — defaults to gemini-2.5-flash (free tier).
GEMINI_MODEL=gemini-2.5-flash
