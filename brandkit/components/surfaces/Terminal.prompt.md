macOS-chrome terminal window for code — Fira Code 13px over `#0b1018`. Color code with the global token classes.

```jsx
<Terminal title="rag-pipeline.ts">
  <span className="tok-com">{"// embed the knowledge base"}</span>{"\n"}
  <span className="tok-key">const</span> store = <span className="tok-fn">await</span> embed(<span className="tok-str">"docs/**"</span>);
</Terminal>
```

Token classes (global CSS): `tok-key` cyan · `tok-fn` purple · `tok-str` green · `tok-num` ice · `tok-com` muted italic · `tok-punc` zinc.
