The brand's five inline SVG icons — Lucide-style 24-viewBox, 2px round stroke, `currentColor` (GitHub mark is filled).

```jsx
<ArrowRightIcon style={{ width: 16, height: 16 }} />
<GithubIcon className="text-muted" />
<WindowControls />
```

Need more icons? Use Lucide from CDN — it matches this stroke style exactly. Never use emoji.
