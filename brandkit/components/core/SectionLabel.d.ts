export interface SectionLabelProps {
  /** Two-digit index string, e.g. "01" */
  index: string;
  title: string;
}
/** Numbered uppercase eyebrow with a cyan fade rule. */
export declare function SectionLabel(props: SectionLabelProps): JSX.Element;
