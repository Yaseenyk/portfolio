Mono terminal-path section heading — cyan uppercase `~/path`, fading zinc rule, optional right-aligned action link.

```jsx
<TerminalPath path="~/projects" />
<TerminalPath path="~/field-notes" action="View all →" actionHref="/blog" />
```

Section names follow the path convention: `~/projects`, `~/experience`, `~/field-notes`.
