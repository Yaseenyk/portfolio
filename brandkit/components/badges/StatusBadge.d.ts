export interface StatusBadgeProps {
  children?: React.ReactNode;
}
/** Pill with live PulseDot — hero availability/role badge. */
export declare function StatusBadge(props: StatusBadgeProps): JSX.Element;
