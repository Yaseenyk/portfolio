import React from "react";

/** Ice metric pill with glowing dot — quantified claims ("94% Payload Compression"). */
export function MetricPill({ children }) {
  return <span className="sk-metric-pill">{children}</span>;
}
