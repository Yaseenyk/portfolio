import React from "react";

/** Emerald mono callout for a quantified ROI/result claim. */
export function RoiNote({ children }) {
  return <div className="sk-roi-note"><span>{children}</span></div>;
}
